[tool.poetry]
name = "edc-backend"
version = "0.1.0"
description = ""
authors = ["ASTA"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
